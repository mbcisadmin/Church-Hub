# Claude Code Skills

Skills for Church Hub development. Template-specific skills live here; shared
skills (like MinistryPlatform) are in the parent `.claude/skills/` folder.

## Template-Specific Skills (this folder)

### `general/`

Architecture and template skills:

- `/new-micro-app` - Create a new micro-app
- `/mark-upstream` - Document template bug fixes for backporting

### `neon/`

Neon PostgreSQL skills (coming soon)

## Shared Skills (parent folder)

MinistryPlatform skills are shared across all client projects and live in
`/Users/coltonwirgau/Development/clients/.claude/skills/ministry-platform/`:

- `/new-stored-proc` - Create MP stored procedure
- `/register-api-proc` - Register procedure in MP API
- `/new-entity` - Create Zod schema for MP table
- `/new-api-route` - Create API route for MP data
- `/mp-troubleshoot` - Debug MP API issues
